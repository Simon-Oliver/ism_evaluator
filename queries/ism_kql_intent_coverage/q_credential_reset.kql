AuditLogs
| where TimeGenerated > ago(90d)
| where OperationName has_any ("Reset password", "Change password", "Update user", "Authentication method")
| summarize Events=count(), LastSeen=max(TimeGenerated) by OperationName, Result
