union isfuzzy=true AzureDiagnostics, CommonSecurityLog
| where TimeGenerated > ago(14d)
| where tostring(pack_all()) has_any ("proxy", "URL", "http", "https", "blocked", "allowed")
| summarize Events=count(), LastSeen=max(TimeGenerated) by Type, Action=tostring(column_ifexists("Action", ""))
