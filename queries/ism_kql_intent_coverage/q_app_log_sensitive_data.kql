union isfuzzy=true SecurityAlert, AzureDiagnostics
| where TimeGenerated > ago(30d)
| where tostring(pack_all()) has_any ("sensitive", "secret", "password", "token", "credential", "PII", "DLP")
| summarize Events=count(), LastSeen=max(TimeGenerated) by Type
