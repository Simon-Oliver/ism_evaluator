union isfuzzy=true AzureDiagnostics, CommonSecurityLog, Syslog
| where TimeGenerated > ago(30d)
| where tostring(pack_all()) has_any ("DoH", "DNS over HTTPS", "DNS over TLS", "encrypted DNS")
| summarize Events=count(), LastSeen=max(TimeGenerated) by Type
