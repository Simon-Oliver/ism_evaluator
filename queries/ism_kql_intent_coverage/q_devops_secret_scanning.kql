union isfuzzy=true SecurityAlert, AzureDiagnostics, AzureActivity
| where TimeGenerated > ago(30d)
| where tostring(pack_all()) has_any ("secret scanning", "credential", "token", "PAT", "private key", "commit", "repository")
| summarize Events=count(), LastSeen=max(TimeGenerated) by Type
