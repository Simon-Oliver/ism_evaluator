union isfuzzy=true AzureDiagnostics, AzureActivity, SigninLogs
| where TimeGenerated > ago(30d)
| where tostring(pack_all()) has_any ("Bastion", "jump", "privileged access workstation", "admin workstation")
| summarize Events=count(), LastSeen=max(TimeGenerated) by Type
